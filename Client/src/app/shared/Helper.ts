import { environment } from 'src/environments/environment';

export class ApiHelper {
    static clashRoyaleImageApi = environment.clashRoyaleImageApi;

    static clientRoot = environment.clientRoot;
    static clashRoyaleApi = environment.clashRoyaleApi;
    static stsAuthority = environment.stsAuthority;

    static clientId = environment.clientId;
}